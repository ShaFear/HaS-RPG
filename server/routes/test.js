/*require("../app.js");

app.get('/awesome', function(req, res) {
  connection.query('SHOW COLUMNS FROM characters', function(err, rows, fields) {
    if (!err){
      res.send(JSON.stringify(rows));
    }
    else
      res.send('Fuck You!');
  });

});
*/
